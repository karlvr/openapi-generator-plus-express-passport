{{>header}}

{{#each groups}}
import * as {{identifier name}} from './api/{{identifier name}}/types'
{{/each}}

export interface ApiImplementation {
	{{#each groups}}
	{{identifier name}}: {{identifier name}}.{{className name}}Api
	{{/each}}
}
