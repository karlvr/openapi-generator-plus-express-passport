{
	"name": "{{name}}",
	{{#if private}}
	"private": true,
	{{/if}}
	"version": "{{version}}",
	"description": "API client for {{name}}",
	"author": "@openapi-generator-plus/typescript-fetch-client-generator",
	"keywords": [
		"fetch",
		"typescript",
		"swagger",
		"openapi",
		"{{name}}"
	],
	"license": "UNLICENSED",
	"main": "./dist/index.js",
	"typings": "./dist/index.d.ts",
	"scripts": {
		"build": "tsc",
		"prepare": "npm run build"
	},
	"dependencies": {
{{#join '__dependencies' ',
		'}}
{{>hooks/packageDependencies}}
{{#ifeq dateApproach 'blind-date'}}
"blind-date": "^3.3.0"
{{/ifeq}}
"express": "^4.18.2"
"passport": "^0.6.0"
"multer": "^1.4.5-lts.1"
{{/join}}
		{{{__dependencies}}}
	},
	"devDependencies": {
		"@types/express": "^4.17.17",
		"@types/node": "^18.15.11",
		"@types/passport": "^1.0.12",
		"@types/multer": "^1.4.11",
		"typescript": "^4.9.5"
	{{#if repository}}
	},
	"publishConfig": {
		"registry": "{{{repository}}}"
	{{/if}}
	}
}
