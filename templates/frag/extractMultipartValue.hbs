{{!--
Extract the correct value from the multipart input for use with the function from fromMultipart.
@param <this> CodegenProperty or CodegenSchema
--}}
{{#if (isFile schema)}}
value
{{~else if (isMetadataSchema schema)~}}
value.files[{{{stringLiteral serializedName}}}]
{{~else~}}
value.body[{{{stringLiteral serializedName}}}]
{{~/if}}
