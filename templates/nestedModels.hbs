{{#each schemas}}
	{{#unless (isMultipartSchemaPart .)}}
	{{#if (isEnum)}}
	{{>modelEnum}}
	{{else if (isObject)}}
	{{>modelGeneric}}
	{{else if (isOneOf)}}
	{{>modelOneOf}}
	{{else if (isAnyOf)}}
	{{>modelOneOf}}
	{{else if (isInterface)}}
	{{>modelInterface}}
	{{/if}}

	{{/unless}}
{{/each}}
