const {{constantName (concat nativeType 'Keys')}}: string[] = [{{#each properties}}{{{stringLiteral serializedName}}}{{#hasMore}}, {{/hasMore}}{{/each}}]

function model{{className nativeType.parentType}}FromJsonContent(name: string, value: any, knownKeys: Record<string, boolean> = {}): {{#if discriminator}}Omit<{{nativeType.parentType}}, {{{stringLiteral discriminator.name}}}>{{else}}{{nativeType}}{{/if}} {
	if (typeof value !== 'object' || value === undefined || value === null) {
		throw `Invalid type for ${name}: expected object got ${typeof value}`
	}

	{{constantName (concat nativeType 'Keys')}}.forEach(k => knownKeys[k] = true)

	const result: {{#if discriminator}}Omit<{{nativeType.parentType}}, {{{stringLiteral discriminator.name}}}>{{else}}{{nativeType}}{{/if}} = {
	{{#each parents}}
		...{{>frag/fromJson . prefix=''}}Content(name, value, knownKeys),
	{{/each}}
	{{#each properties}}
	{{#unless readOnly}}
	{{#if required}}
	{{#if nullable}}
		{{{stringLiteral serializedName}}}: allowNull({{>frag/fromJson prefix=''}})(`${name}.{{serializedName}}`, value[{{{stringLiteral serializedName}}}]),
	{{else}}
		{{{stringLiteral serializedName}}}: {{>frag/fromJson prefix=''}}(`${name}.{{serializedName}}`, value[{{{stringLiteral serializedName}}}]),
	{{/if}}
	{{else}}
	{{#if nullable}}
		{{{stringLiteral serializedName}}}: allowNullOrUndefined({{>frag/fromJson prefix=''}})(`${name}.{{serializedName}}`, value[{{{stringLiteral serializedName}}}]),
	{{else}}
		{{{stringLiteral serializedName}}}: allowUndefined({{>frag/fromJson prefix=''}})(`${name}.{{serializedName}}`, value[{{{stringLiteral serializedName}}}]),
	{{/if}}
	{{/if}}
	{{/unless}}
	{{/each}}
	{{#each discriminatorValues}}
		{{{stringLiteral model.discriminator.name}}}: {{{value}}},
	{{/each}}
	}

	return result
}

function model{{className nativeType.parentType}}ToJsonContent(name: string, value: {{nativeType}}, knownKeys: Record<string, boolean> = {}): ToJson<{{#if discriminator}}Omit<{{nativeType.parentType}}, {{{stringLiteral discriminator.name}}}>{{else}}{{nativeType}}{{/if}}> {
	if (typeof value !== 'object' || value === undefined || value === null) {
		throw `Invalid type for ${name}: expected object got ${typeof value}`
	}

	{{constantName (concat nativeType 'Keys')}}.forEach(k => knownKeys[k] = true)
	
	const result: ToJson<{{#if discriminator}}Omit<{{nativeType.parentType}}, {{{stringLiteral discriminator.name}}}>{{else}}{{nativeType}}{{/if}}> = {
	{{#each parents}}
		...{{>frag/toJson . prefix=''}}Content(name, value, knownKeys),
	{{/each}}
	{{#each properties}}
	{{#unless writeOnly}}
	{{#if required}}
	{{#if nullable}}
		{{{stringLiteral serializedName}}}: allowNull({{>frag/toJson prefix=''}})(`${name}.{{serializedName}}`, value[{{{stringLiteral serializedName}}}]),
	{{else}}
		{{{stringLiteral serializedName}}}: {{>frag/toJson prefix=''}}(`${name}.{{serializedName}}`, value[{{{stringLiteral serializedName}}}]),
	{{/if}}
	{{else}}
	{{#if nullable}}
		{{{stringLiteral serializedName}}}: allowNullOrUndefined({{>frag/toJson prefix=''}})(`${name}.{{serializedName}}`, value[{{{stringLiteral serializedName}}}]),
	{{else}}
		{{{stringLiteral serializedName}}}: allowUndefined({{>frag/toJson prefix=''}})(`${name}.{{serializedName}}`, value[{{{stringLiteral serializedName}}}]),
	{{/if}}
	{{/if}}
	{{/unless}}
	{{/each}}
	{{#each discriminatorValues}}
		{{{stringLiteral model.discriminator.name}}}: {{{value}}},
	{{/each}}
	}

	return result
}

export function model{{className nativeType.parentType}}FromJson(name: string, value: any): {{nativeType}} {
	{{#if discriminator}}
	const discriminatorValue = value.{{discriminator.name}}
	switch (value.{{discriminator.name}}) {
		{{#each discriminator.references}}
		case {{{value}}}:
			return {{>frag/fromJson model prefix=''}}(name, value)
		{{/each}}
	}

	throw `Invalid value for ${name}: didn't contain a known discriminator value: ${discriminatorValue}`
	{{else}}
	const knownKeys: Record<string, boolean> = {}
	const result: {{nativeType}} = model{{className nativeType.parentType}}FromJsonContent(name, value, knownKeys)

	/* Known keys */
	// TODO if we don't ignore unknown properties
	for (const key of Object.keys(value)) {
		if (!knownKeys[key]) {
			// throw `Unexpected key: ${key}`
			console.warn(`Unexpected key in {{nativeType}}: ${key}`)
		}
	}

	return result
	{{/if}}
}

export function model{{className nativeType.parentType}}ToJson(name: string, value: {{nativeType}}): ToJson<{{nativeType}}> {
	{{#if discriminator}}
	const discriminatorValue = value.{{discriminator.name}}
	switch (value.{{discriminator.name}}) {
		{{#each discriminator.references}}
		case {{{value}}}:
			return {{>frag/toJson model prefix=''}}(name, value)
		{{/each}}
	}

	throw `Invalid value for ${name}: didn't contain a known discriminator value: ${discriminatorValue}`
	{{else}}
	const knownKeys: Record<string, boolean> = {}
	const result: ToJson<{{nativeType}}> = model{{className nativeType.parentType}}ToJsonContent(name, value, knownKeys)

	/* Known keys */
	// TODO if we don't ignore unknown properties
	for (const key of Object.keys(value)) {
		if (!knownKeys[key]) {
			// throw `Unexpected key: ${key}`
			console.warn(`Unexpected key in {{nativeType}}: ${key}`)
		}
	}

	return result
	{{/if}}
}
{{>nestedValidation}}
